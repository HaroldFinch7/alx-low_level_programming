#include "main.h"
#include <stdio.h>

/**
 * _puts - Locates a substring.
 * @s: The string to be searched.
 *
 * Return: nothing
 */

void _puts(char *s)
{
	puts(s);
}

int main(void)
{
	_puts("print this");
	return (0);
}
